const CACHE='player-v1';const ASSETS=['/player/player.css'];self.addEventListener('install',e=>{e.waitUntil(caches.open(CACHE).then(c=>c.addAll(ASSETS)));self.skipWaiting();});self.addEventListener('fetch',e=>{const url=new URL(e.request.url);if(ASSETS.includes(url.pathname)||url.pathname.startsWith('/player/')){e.respondWith(caches.match(e.request).then(r=>r||fetch(e.request)));}});